language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t eoin-barr/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true eoin-barr/docker-react npm run test
